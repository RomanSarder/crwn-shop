import styled from 'styled-components'

export default styled.button`
    display: inline-flex;
    background: none;
    border:none;
    appearance: none;

    &:focus {
        outline: none;
    }
`